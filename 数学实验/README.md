# 数学实验

这里集中保存为了理解数学概念而编写的可运行代码、Notebook 和有意义的结果图。实验仍然属于同一个学习项目，不与笔记和教材做物理分离。

## 当前主题

- `单纯同调/`：从单纯复形、边缘算子到同调群的计算与可视化。
- `反演几何/`：复平面和圆反演的静态图与交互实验。

每个主题把源码、说明和可复现结果放在一起。Python 缓存、调试日志和编辑器状态由 Git 忽略。

第一次运行实验前，在项目根目录安装数学实验的可选依赖：

```bash
uv pip install --python .venv/bin/python -e '.[math]'
```
