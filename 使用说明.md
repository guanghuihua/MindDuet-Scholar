# MindDuet Scholar 使用说明

MindDuet Scholar 是一个本地优先的个人数学学习系统。它把本仓库中的数学笔记、教材、习题、论文材料和学习应用放在同一个项目里，并用本地索引和学习数据库记录真实的学习过程。

它目前由两部分配合工作：

- **MindDuet Scholar 网页**：搜索本地资料、阅读 PDF、保存学习尝试、请求小提示、安排复习和查看学习报告。
- **Codex 学习助手**：读取项目资料和 PDF 阅读上下文，翻译、解释、检查证明、诊断知识缺口并生成迁移问题。

PDF Reader 和 `Ask & Understand` Session 中的 Codex 对话会自动保存在本地数据库。需要作为掌握证据的独立证明尝试和复习结果，仍应使用 `Practice & Prove` Session 保存。

## 1. 环境要求

推荐环境：

- WSL 或 Linux；
- Python 3.12 或更高版本；
- VS Code；
- 一个现代浏览器。

项目已经有 `.venv/` 时，直接使用它即可。首次安装或需要重建虚拟环境时，在项目根目录执行：

```bash
python3.12 -m venv .venv
source .venv/bin/activate
pip install -e ".[dev]"
```

## 2. 启动项目

进入项目目录：

```bash
cd /home/guanghui/projects/MindDuet-Scholar
```

第一次使用，或者资料发生较大变化时，先建立索引：

```bash
.venv/bin/mindduet index
```

启动网页：

```bash
.venv/bin/mindduet serve --host 127.0.0.1 --port 8000
```

然后打开：

```text
http://127.0.0.1:8000
```

如果 `8000` 已被其他程序占用，可以改用 `8001`：

```bash
.venv/bin/mindduet serve --host 127.0.0.1 --port 8001
```

对应地址为：

```text
http://127.0.0.1:8001
```

启动服务的终端需要保持运行。需要停止服务时，在该终端按 `Ctrl+C`。

## 3. 更新资料索引

索引器默认扫描整个项目，支持以下资料类型：

- Markdown：`.md`、`.markdown`；
- LaTeX：`.tex`；
- Jupyter Notebook：`.ipynb`；
- PDF：`.pdf`。

有两种更新方式。

方式一：在首页点击 `Scan source library`。

方式二：在终端执行：

```bash
.venv/bin/mindduet index
```

索引只读取源文件，不修改原始笔记和教材。新增、删除或重命名资料后，应当重新索引。如果搜索不到刚放入项目的文件，第一步也应当是重新索引。

## 4. 网页页面

### Today

首页显示：

- 已索引的资料数量；
- 正在进行的学习会话数量；
- 已到期的复习数量；
- 最近的学习会话；
- 重新扫描资料库的入口。

### Library

`Library` 是本地资料搜索页。可以按标题、路径或已提取文本搜索，例如：

```text
Linear Algebra Done Right
```

```text
同调群
```

```text
Hungerford
```

点击搜索结果可以查看资料详情。PDF 资料会显示 `Open reader`，所有资料都可以通过 `Study this source` 创建关联的学习会话。

### Sessions

`Sessions` 保存围绕一个学习目标展开的长期记录。创建时先选择模式：

- `Ask & Understand`：直接向 Codex 提问并连续追问，不要求先写 attempt；提问和回答会保存在该 Session 中，刷新页面后仍然存在。
- `Practice & Prove`：先独立写定义、计算或证明，再请求小提示，并把过程作为学习证据。

一次会话最好只设置一个明确目标，例如：

```text
不看答案证明群论第一同构定理
```

而不是过于宽泛的：

```text
学习抽象代数
```

创建会话时可以指定：

- `Session mode`：选择直接理解或独立练习；
- `Learning goal`：本次学习目标；
- `Source document`：关联的教材或笔记；
- `Section or page reference`：章节、页码或题号。

选择 `Ask & Understand` 后，可以直接在对话框中输入问题，例如：

```text
为什么 Z/nZ 与 Z_n 同构？这里的元素分别是什么？
```

按 `Send` 发送，也可以使用 `Ctrl+Enter` 或 `Cmd+Enter`。回答完成后，提问和回答都写入 `.mindduet/mindduet.sqlite3`。每次请求使用临时 Codex thread，因此不会在普通 Codex 历史列表中生成一条新记录。

对话内容会作为网页正文自然展开，不再使用独立的聊天滚动条。滚轮、触控板、浏览器右侧滚动条以及 `PageUp`、`PageDown`、`Home`、`End` 都直接控制整个网页。

选择 `Practice & Prove` 后，推荐按以下顺序操作：

1. 在 `Attempt` 中写下自己的定义、计算或证明步骤。
2. 点击 `Save attempt` 保存尝试。
3. 卡住时点击 `Request one hint`，只请求一个小提示。
4. 修改思路后再次保存新的尝试，不覆盖以前的过程。
5. 点击 `Schedule 1d + 7d reviews` 安排一天后和七天后的复习。
6. 本次目标暂时结束后点击 `Complete session`。
7. 之后产生新问题时，打开原 Session 并点击 `Reopen session`；它会恢复输入功能，同时保留全部历史记录。

`Complete session` 表示当前学习阶段暂时结束，不是永久封存。重新打开时不需要创建重复的学习目标。

### Reports

`Reports` 汇总：

- 已记录的错误或诊断信号；
- 基于尝试数量形成的掌握证据；
- 等待完成的间隔复习。

完成复习时，应在输入框中写下自己独立回忆出的内容。看过答案之后的熟悉感，不应当作为掌握证据。

## 5. 使用 PDF Reader

在 `Library` 中找到 PDF，进入详情页并点击 `Open reader`。

Reader 支持：

- 上一页和下一页；
- 直接输入页码；
- 调整缩放比例；
- 选择 PDF 中的文本；
- 在右侧输入自己的问题或备注；
- 保存当前文件、页码、缩放比例、选中文本和当前页文本。
- 在 Reader 内直接与本机 Codex 对话并显示 LaTeX。

阅读到不懂的位置时：

1. 翻到对应页面。
2. 用鼠标选择不懂的句子、定义或证明步骤。
3. 在右侧选择 `Codex` 标签。
4. 输入问题，或者点击 `Explain`、`Translate`、`Check me` 快捷按钮。
5. 点击 `Send`，也可以按 `Ctrl+Enter` 或 `Cmd+Enter`。

```text
解释当前 PDF
```

发送前，Reader 会自动保存当前页和选择内容，所以不再需要切回 VS Code。需要手动修改选择内容或备注时，切换到右侧 `Context` 标签；点击工具栏的 `Save selection` 也可以只保存上下文而不提问。

Codex 回答时：

- 方形停止按钮用于中断当前回答；
- `+` 按钮归档当前对话并创建新对话；
- 每一本 PDF 分别保存自己的活动 conversation；
- 提问和回答保存在 `.mindduet/mindduet.sqlite3`，不会提交到 Git；
- 每次请求使用不会出现在 Codex 历史列表中的临时 thread；
- Codex 默认使用只读沙箱，不能通过这个页面修改项目文件。

Reader 会把当前上下文保存在：

```text
.mindduet/current_pdf_context.json
```

内嵌 Codex 会读取其中的文件路径、页码、选中内容和当前页文本，并用中文解释。页面切换时会自动保存当前页。

可以使用更具体的提问：

```text
翻译当前选中的段落，并解释每个数学符号。
```

```text
解释当前证明依赖哪些定理，只给我第一个提示。
```

```text
检查我对当前定理的理解，先问我一个问题。
```

```text
证明当前题目的 (ii)，使用 LaTeX，并指出每一步依据。
```

如果选中文本和页面文本都是空的，PDF 很可能是扫描件。此时可以：

- 在右侧手动输入题目或页码；
- 向 Codex 提供截图；
- 指明定理名称、题号或段落位置。

## 6. 推荐的数学学习流程

一次完整学习可以使用下面的闭环。

### 第一步：确定目标

把目标写成可以验证的任务：复述一个定义、完成一次计算、证明一个命题，或者找出一个反例。

### 第二步：先独立尝试

在 `Session` 中先写自己的版本。即使只写出定义和卡住的位置，也比直接阅读完整答案更有学习价值。

### 第三步：请求最小帮助

向 Codex 说明限制，例如：

```text
先不要给完整证明，只指出我最早出错的地方。
```

```text
只给一个可以继续推进的提示。
```

### 第四步：修正并保存

把新的证明作为下一次 attempt 保存，保留原始错误。这样才能观察自己真正反复出现的问题。

### 第五步：迁移与复习

可以让 Codex 生成条件稍有变化的新问题：

```text
根据我刚才的错误，给我一道结构相同但表面不同的题。
```

然后安排一天后和七天后的独立重做。

## 7. 如何与 Codex 配合

PDF Reader 已经通过本机 Codex SDK 连接到这个项目，因此日常读书不需要打开 VS Code Codex 扩展。Reader 使用独立的临时工作目录：

```text
/home/guanghui/projects/MindDuet-Scholar/.mindduet/codex-workspace
```

Reader 和 `Ask & Understand` Session 的临时 Codex thread 都不会出现在 Codex 普通历史记录中。每本 PDF 和每个 inquiry Session 的连续对话由 MindDuet 保存在本地 SQLite，并在下一次请求时注入最近的本地消息，因此不会和你在项目根目录中的正式 Codex 记录混在一起。

适合直接使用的指令包括：

```text
读取当前 PDF，翻译并解释选中的内容。
```

```text
读取项目中与边缘算子有关的笔记，检查我的理解。
```

```text
这是我的证明尝试。不要重写完整证明，先找第一个关键缺口。
```

```text
把这个群论结论和线性代数中的核、像、商空间联系起来。
```

```text
根据我今天学过的内容，给我一道睡前思考题。
```

```text
为这次学习生成一天后和七天后的回忆问题。
```

数学表达默认使用 LaTeX。对于复杂证明，可以明确要求使用当前环境中可用的高能力推理模型；实际模型是否能够切换，由当前 Codex/OpenAI 运行环境决定。

内嵌助手复用本机 Codex 登录。首次使用前可以在终端检查：

```bash
codex --version
codex login status
```

如果尚未登录，运行：

```bash
codex login
```

## 8. 可选 AI 提示配置

即使不配置 API，网页仍可提供本地的基础一级提示。配置兼容 OpenAI Responses API 的服务后，网页中的 `Request one hint` 可以请求远程模型。

在启动服务之前设置环境变量：

```bash
export MINDDUET_AI_API_KEY="你的本地环境变量值"
export MINDDUET_AI_MODEL="模型标识"
```

如果使用其他兼容服务，还可以设置：

```bash
export MINDDUET_AI_BASE_URL="https://example.com/v1"
```

不要把真实 API key 写入仓库、Markdown、`.env` 提交记录或截图。网页的提示模型配置与当前 Codex 对话所使用的模型是两个独立设置。

内嵌 Codex 默认使用本机 Codex 当前配置的模型。如果需要覆盖，可以在启动 MindDuet Scholar 前设置：

```bash
export MINDDUET_CODEX_MODEL="模型标识"
```

通常不需要设置这一项；不存在或当前账户不可用的模型会导致 Reader 显示模型不可用错误。

## 9. 本地数据与 Git

运行时数据存放在 `.mindduet/`，主要包括：

- SQLite 学习数据库；
- 资料索引；
- 当前 PDF 阅读上下文。

这些内容属于本机状态，已经被 `.gitignore` 忽略。`.venv/`、缓存、API 密钥、编辑器状态和 LaTeX 编译中间文件也不会进入 Git。

大型教材 PDF 可以留在本机并加入 `.gitignore`，不需要为了保持 Git 仓库较小而删除本地教材。提交或推送前建议检查：

```bash
git status --short --branch
```

## 10. 常见问题

### `mindduet: command not found`

使用虚拟环境中的完整路径：

```bash
.venv/bin/mindduet --help
```

如果该文件不存在，重新执行安装步骤：

```bash
source .venv/bin/activate
pip install -e ".[dev]"
```

### 端口已经被占用

换一个端口，例如：

```bash
.venv/bin/mindduet serve --host 127.0.0.1 --port 8001
```

### 新资料搜索不到

重新建立索引：

```bash
.venv/bin/mindduet index
```

然后刷新 `Library` 页面。还应确认文件扩展名属于索引器支持的类型。

### PDF 打不开或触发下载

先刷新 Reader；仍不能显示时，展开页面底部的 `Open native PDF fallback`。也可以直接从 VS Code 打开原始 PDF，但 VS Code 自带阅读器的位置不会自动同步给 Codex。

### PDF 显示模糊

先提高 Reader 的缩放比例，并和 `Open native PDF fallback` 对比。如果两者都模糊，原文件很可能是低分辨率扫描件。

### Codex 解释了错误的页面

回到 Reader，确认页码，重新选择文本并点击 `Save selection`，然后再说“解释当前 PDF”。

### `Request one hint` 只返回通用问题

这通常表示没有配置远程 AI，或者当前 attempt 太短。先写清楚目标、已使用的定义和卡住的步骤；需要更深入的解释时，直接在 Codex 对话中提供尝试。

## 11. 开发与测试

运行测试：

```bash
.venv/bin/pytest
```

主要代码位于：

```text
mindduet_scholar/
```

测试位于：

```text
tests/
```

产品设计与长期路线见 `MINDDUET_SCHOLAR.md`。项目协作规则和当前学习背景见 `AGENTS.md`。

## 12. 当前能力边界

目前已经实现：资料索引与搜索、PDF 阅读上下文、Reader 内嵌只读 Codex、临时 thread、流式回答、LaTeX 显示、按 PDF 保存对话、`Ask & Understand` 连续问答、`Practice & Prove` 尝试历史、一级提示、简单诊断、间隔复习和报告页面。

目前尚未完整实现：扫描 PDF 的自动 OCR、把 Codex 对话自动转成 mastery 或 misconception 证据、网页审批文件修改、完整知识图谱、形式化证明检查和复杂的模型自动路由。内嵌 Codex 对话会被保存，但不会自动提高掌握度；真正的独立尝试仍应保存到 `Sessions`。
